{
  "name": "sid",
  "version": "0.1.0",
  "description": "Static IP Dispenser is a web service that monitors dnsmasq activity and allows you to easily assign a consistent IP address to a particular device that requests and address from dnsmasq.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "lint": "eslint src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@pi2:sid"
  },
  "keywords": [
    "dnsmasq",
    "dns",
    "dhcp",
    "ip"
  ],
  "author": "elcarpie@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^3.18.0",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "express": "^4.15.2"
  }
}
